{
  "private": true,
  "name": "pobparser",
  "version": "1.0.0",
  "description": "Parse POB Data into readable formats",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "AtroCty <atrocty@gmx.de>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "cloudflare-worker-rest-api": "^1.0.0",
    "pako": "^1.0.11",
    "xmldom": "^0.4.0",
    "xpath": "0.0.32"
  }
}
